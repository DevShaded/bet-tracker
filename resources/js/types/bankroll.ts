export type Bankroll = {
    id: string,
    user_id: string,
    name: string,
    currency: string,
    starting_balance: number,
    is_active: boolean,
    created_at: string,
    updated_at: string,
};
